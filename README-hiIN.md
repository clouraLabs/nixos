# NixOS इंटरैक्टिव इंस्टालर

[![NixOS](https://img.shields.io/badge/NixOS-5277C3?style=for-the-badge&logo=nixos&logoColor=white)](https://nixos.org)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

डेस्कटॉप वातावरण, डेवलपमेंट टूल्स, गेमिंग, मल्टीमीडिया, क्लाउड सेवाओं और सुरक्षा मजबूती के लिए flakes आधारित NixOS कॉन्फ़िगरेशन जेनरेट करने के लिए एक पूर्ण उपकरण।

## ✨ सुविधाएँ

### 🎨 डेस्कटॉप वातावरण
- **8 विकल्प**: GNOME, KDE Plasma, XFCE, Cinnamon, MATE, i3, Hyprland या कोई नहीं
- प्रत्येक वातावरण के लिए अनुकूलित कॉन्फ़िगरेशन

### 🛠️ डेवलपमेंट टूल्स
- एडिटर्स: vim, neovim, VSCodium, Zed
- प्रोग्रामिंग लैंग्वेज: Python, Node.js, Rust, Go
- वर्जन कंट्रोल: Git, GitHub CLI
- यूटिलिटीज: tmux, ripgrep, fzf, direnv

### 🤖 आर्टिफिशियल इंटेलिजेंस
- **aichat**: यूनिवर्सल AI CLI, Claude, GPT, Gemini समर्थन
- शेल शॉर्टकट (`ai`, `claude`, `gpt`)
- एकाधिक प्रदाता समर्थन

### 🎮 गेमिंग
- Steam with Proton
- Lutris और Heroic Launcher
- परफॉरमेंस टूल्स (MangoHud, GameMode)
- Vulkan और ग्राफिक्स drivers

### 🎨 मल्टीमीडिया
- वीडियो एडिटिंग: Kdenlive, OBS Studio
- इमेज एडिटिंग: GIMP, Krita, Blender
- ऑडियो प्रोडक्शन: Audacity, Ardour

### ☁️ क्लाउड सेवाएँ
- AWS CLI, Azure CLI, Google Cloud SDK
- Kubernetes (kubectl, Helm, k9s)
- Terraform और Ansible

### 🔒 सुरक्षा मजबूती
- फायरवॉल, AppArmor, fail2ban
- SSH hardening
- ClamAV एंटीवायरस

## 🚀 इंस्टालेशन

### पूर्वापेक्षाएँ

- NixOS सिस्टम (या live वातावरण)
- Bash shell
- sudo अधिकार
- इंटरनेट कनेक्शन

### त्वरित इंस्टालेशन

1. **रिपॉजिटरी क्लोन करें:**
   ```bash
   git clone https://github.com/your-username/nixos.git
   cd nixos
   ```

2. **इंस्टालर चलाएँ:**
   ```bash
   ./install.sh        # अंग्रेजी
   ./install-ptBR.sh   # पुर्तगाली (ब्राज़ीली)
   ./install-zhCN.sh   # चीनी (सरलीकृत)
   ./install-hiIN.sh   # हिंदी
   ```

3. **इंटरैक्टिव प्रॉम्प्ट्स का जवाब दें:**
   - अपना डेस्कटॉप चुनें
   - आवश्यक सुविधाएँ चुनें
   - अतिरिक्त उपयोगकर्ता कॉन्फ़िगर करें

4. **कॉन्फ़िगरेशन सक्रिय करें:**
   ```bash
   sudo nixos-rebuild switch --flake .#आपका-होस्टनाम
   ```

## 📋 फाइल संरचना

```
nixos/
├── install.sh                      # मुख्य इंस्टालर (अंग्रेजी)
├── install-ptBR.sh                 # ब्राज़ीली पुर्तगाली संस्करण
├── install-zhCN.sh                 # चीनी संस्करण
├── install-hiIN.sh                 # हिंदी संस्करण
├── flake.nix                       # Flakes कॉन्फ़िगरेशन (जनरेटेड)
├── hosts/                          # होस्ट-विशिष्ट कॉन्फ़िगरेशन
│   └── hostname/
│       ├── configuration.nix       # मुख्य कॉन्फ़िग
│       └── hardware-configuration.nix
├── modules/                        # पुन: प्रयोज्य मॉड्यूल
│   ├── system.nix                  # आधार सिस्टम
│   ├── desktop-gnome.nix           # GNOME डेस्कटॉप
│   ├── development.nix             # डेव टूल्स
│   └── ...
├── users/                          # उपयोगकर्ता कॉन्फ़िगरेशन
│   └── username/
│       └── home.nix                # Home Manager
├── docs/                           # डॉक्यूमेंटेशन
├── QUICKSTART.md                   # क्विक स्टार्ट गाइड
├── FEATURES.md                     # सुविधाएँ विस्तार से
├── AI-TOOLS-SETUP.md              # AI टूल्स गाइड
├── CHANGELOG.md                    # परिवर्तन इतिहास
└── README.md                       # यह फाइल
```

## 🎯 उपयोग मामले

### डेवलपर वर्कस्टेशन
```bash
# चुनाव: GNOME + डेव टूल्स + Docker + AI + क्लाउड
डेस्कटॉप: GNOME
✓ डेवलपमेंट टूल्स
✓ Docker
✓ AI टूल्स
✓ क्लाउड CLI
```

### गेमिंग PC
```bash
# चुनाव: KDE Plasma + गेमिंग + मल्टीमीडिया
डेस्कटॉप: KDE Plasma
✓ गेमिंग टूल्स
✓ मल्टीमीडिया प्रोडक्शन
✓ डेवलपमेंट टूल्स
```

### सर्वर
```bash
# चुनाव: कोई नहीं + Docker + क्लाउड + सुरक्षा
डेस्कटॉप: कोई नहीं
✓ Docker
✓ क्लाउड CLI
✓ सुरक्षा मजबूती
```

## 🛠️ उपयोगी कमांड्स

```bash
# बिल्ड और सक्रिय करें
make switch
# या
sudo nixos-rebuild switch --flake .#hostname

# बिना हस्तांतरण के परीक्षण करें
make test
# या
sudo nixos-rebuild test --flake .#hostname

# पैकेज अपडेट करें
make upgrade
# या
nix flake update && sudo nixos-rebuild switch --flake .#hostname

# पुरानी जनरेशन साफ करें
make clean
# या
sudo nix-collect-garbage -d

# कॉन्फ़िगरेशन सत्यापित करें
make check
# या
nix flake check
```

## 🔧 कस्टमाइजेशन

### सिस्टम पैकेज जोड़ें

`hosts/hostname/configuration.nix` संपादित करें:
```nix
environment.systemPackages = with pkgs; [
  vim
  wget
  # यहाँ आपके पैकेज
  firefox
  thunderbird
];
```

### उपयोगकर्ता पैकेज जोड़ें

`users/username/home.nix` संपादित करें:
```nix
home.packages = with pkgs; [
  # यहाँ आपके पैकेज
  discord
  spotify
];
```

### नए मॉड्यूल बनाएँ

`modules/custom.nix` बनाएँ:
```nix
{ config, pkgs, ... }:

{
  # यहाँ आपकी कस्टम कॉन्फ़िगरेशन
}
```

फिर `configuration.nix` में इंपोर्ट करें।

## 🐛 समस्या समाधान

### "flake not found" त्रुटि
```bash
cd /path/to/your/config
sudo nixos-rebuild switch --flake .#hostname
```

### अनुमति त्रुटि
```bash
# उपयोगकर्ता को wheel समूह में जोड़ें
sudo usermod -aG wheel आपका-उपयोगकर्ता-नाम
```

### रोलबैक
```bash
# पिछली जनरेशन पर वापस जाएँ
sudo nixos-rebuild switch --rollback
```

### विस्तृत लॉग
```bash
sudo nixos-rebuild switch --flake .#hostname --show-trace
```

## 📚 अतिरिक्त डॉक्यूमेंटेशन

- **[QUICKSTART.md](QUICKSTART.md)** - क्विक स्टार्ट गाइड
- **[FEATURES.md](FEATURES.md)** - सुविधाएँ विस्तार से
- **[AI-TOOLS-SETUP.md](AI-TOOLS-SETUP.md)** - AI टूल्स कॉन्फ़िगरेशन
- **[NixOS मैनुअल](https://nixos.org/manual/nixos/stable/)** - आधिकारिक डॉक्यूमेंटेशन

## 🤝 योगदान

योगदान स्वागतयोग्य हैं! पुल रिक्वेस्ट भेजने से पहले योगदान दिशानिर्देश पढ़ें।

### योगदान कैसे करें:

1. प्रोजेक्ट को फोर्क करें
2. फीचर के लिए ब्रांच बनाएँ (`git checkout -b feature/AmazingFeature`)
3. परिवर्तनों को कमिट करें (`git commit -m 'Add some AmazingFeature'`)
4. ब्रांच को पुश करें (`git push origin feature/AmazingFeature`)
5. पुल रिक्वेस्ट खोलें

### योगदान क्षेत्र:
- नए डेस्कटॉप वातावरण मॉड्यूल
- अधिक AI टूल्स समर्थन
- सुरक्षा सुधार
- अन्य भाषाओं में अनुवाद
- स्वचालित परीक्षण

## 📄 लाइसेंस

यह प्रोजेक्ट MIT लाइसेंस के अंतर्गत लाइसेंस प्राप्त है - विवरण के लिए [LICENSE](LICENSE) फाइल देखें。

## 🙏 आभार

- [NixOS Community](https://nixos.org/) - अद्भुत ऑपरेटिंग सिस्टम के लिए
- [Home Manager](https://github.com/nix-community/home-manager) - उपयोगकर्ता पैकेज प्रबंधन के लिए
- सभी उपयोग किए गए ओपन-सोर्स टूल्स और प्रोजेक्ट्स के लिए

## 📞 समर्थन

- **Issues**: [GitHub Issues](https://github.com/your-username/nixos/issues)
- **Discussions**: [GitHub Discussions](https://github.com/your-username/nixos/discussions)
- **Community**: [NixOS Discourse](https://discourse.nixos.org/)

---

**NixOS का आनंद लें!** 🚀

*यह NixOS इंटरैक्टिव इंस्टालर द्वारा स्वचालित रूप से उत्पन्न किया गया*